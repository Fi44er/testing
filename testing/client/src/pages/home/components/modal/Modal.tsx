import React from 'react'
import styles from './Modal.model.css'

type TypeProps = {
    active: boolean
    setActive: void
}

const Modal = ({active, setActive}:TypeProps) => {
    
    return (
        <div>
            
        </div>
    )
}

export default Modal